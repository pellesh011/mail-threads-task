{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx src/main.ts",
    "dev": "tsx watch src/main.ts",
    "reset-recompute": "tsx src/resetRecompute.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@mail-threads/shared": "1.0.0",
    "@prisma/client": "^7.9.1",
    "redis": "^4.7.0"
  }
}
